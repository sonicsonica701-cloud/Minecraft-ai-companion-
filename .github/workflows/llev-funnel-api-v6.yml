name: Llev Funnel API v6

on:
  workflow_dispatch:
    inputs:
      duration_minutes:
        description: "How long to keep the temporary API alive, max 120"
        required: true
        default: "30"
      hostname_suffix:
        description: "Optional Tailscale hostname suffix"
        required: false
        default: "llev-api"

permissions:
  contents: write

jobs:
  approved-api:
    runs-on: windows-latest
    timeout-minutes: 130

    steps:
      - name: Check required secrets
        shell: pwsh
        run: |
          $missing = @()
          if (-not "${{ secrets.TAILSCALE_AUTH_KEY }}") { $missing += "TAILSCALE_AUTH_KEY" }
          if (-not "${{ secrets.LLEV_API_KEY }}") { $missing += "LLEV_API_KEY" }
          if ($missing.Count -gt 0) {
            Write-Host "Missing required repository secret(s): $($missing -join ', ')"
            exit 1
          }

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Install Tailscale
        shell: pwsh
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Connect to Tailscale
        shell: pwsh
        run: |
          $suffix = "${{ github.event.inputs.hostname_suffix }}"
          if (-not $suffix) { $suffix = "llev-api" }
          $hostname = "gh-$suffix-${{ github.run_id }}"
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=$hostname
          Start-Sleep -Seconds 5
          & "$env:ProgramFiles\Tailscale\tailscale.exe" status

      - name: Create tiny approved API
        shell: pwsh
        env:
          LLEV_API_KEY: ${{ secrets.LLEV_API_KEY }}
        run: |
          @'
          import json, os, platform, socket, subprocess, time
          from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
          from urllib.parse import urlparse
          API_KEY = os.environ.get("LLEV_API_KEY", "")
          STARTED = time.time()
          WORKSPACE = os.environ.get("GITHUB_WORKSPACE", os.getcwd())
          def safe_json(handler, status, payload):
              body = json.dumps(payload, indent=2).encode("utf-8")
              handler.send_response(status)
              handler.send_header("content-type", "application/json")
              handler.send_header("content-length", str(len(body)))
              handler.end_headers()
              handler.wfile.write(body)
          def run_approved(args, timeout=10):
              result = subprocess.run(args, cwd=WORKSPACE, capture_output=True, text=True, timeout=timeout)
              return {"returncode": result.returncode, "stdout": result.stdout[-4000:], "stderr": result.stderr[-2000:]}
          class Handler(BaseHTTPRequestHandler):
              def authorised(self):
                  return API_KEY and self.headers.get("x-api-key") == API_KEY
              def do_GET(self):
                  if not self.authorised(): return safe_json(self, 401, {"error": "unauthorised"})
                  route = urlparse(self.path).path
                  if route == "/health": return safe_json(self, 200, {"status": "ok", "uptime_seconds": int(time.time() - STARTED)})
                  if route == "/system-info": return safe_json(self, 200, {"hostname": socket.gethostname(), "platform": platform.platform(), "python": platform.python_version(), "workspace": WORKSPACE})
                  if route == "/repo-files":
                      items = []
                      for name in sorted(os.listdir(WORKSPACE))[:100]:
                          full = os.path.join(WORKSPACE, name)
                          items.append({"name": name, "type": "folder" if os.path.isdir(full) else "file"})
                      return safe_json(self, 200, {"items": items})
                  if route == "/repo-status": return safe_json(self, 200, run_approved(["git", "status", "--short"]))
                  return safe_json(self, 404, {"error": "not found", "allowed": ["/health", "/system-info", "/repo-files", "/repo-status"]})
              def log_message(self, fmt, *args): print("api", self.address_string(), fmt % args)
          ThreadingHTTPServer(("127.0.0.1", 8787), Handler).serve_forever()
          '@ | Set-Content -Path "$env:RUNNER_TEMP\llev_api.py" -Encoding UTF8
          Start-Process -FilePath python -ArgumentList "$env:RUNNER_TEMP\llev_api.py" -WindowStyle Hidden
          Start-Sleep -Seconds 3
          Invoke-WebRequest -UseBasicParsing -Headers @{"x-api-key" = $env:LLEV_API_KEY} http://127.0.0.1:8787/health

      - name: Save integration info and attempt Tailscale Funnel
        shell: pwsh
        continue-on-error: true
        run: |
          $ts = "$env:ProgramFiles\Tailscale\tailscale.exe"
          $status = & $ts status --json | ConvertFrom-Json
          $dns = $status.Self.DNSName.TrimEnd('.')
          $baseUrl = "https://$dns"
          $info = [ordered]@{
            name = "Llev Runner API"
            baseUrl = $baseUrl
            authStyle = "api_key_header"
            authHeader = "x-api-key"
            safeTestPath = "/health"
            availablePaths = @("/health", "/system-info", "/repo-files", "/repo-status")
            runId = "${{ github.run_id }}"
            createdAt = (Get-Date).ToUniversalTime().ToString("o")
          }
          $info | ConvertTo-Json -Depth 5 | Set-Content -Path "llev-funnel-info.json" -Encoding UTF8
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add llev-funnel-info.json
          git commit -m "Update Llev Funnel integration info"
          if ($LASTEXITCODE -ne 0) { Write-Host "No integration info changes to commit or commit failed." }
          git push origin HEAD:main
          if ($LASTEXITCODE -ne 0) { Write-Host "Could not push integration info, continuing anyway." }
          Write-Host "CUSTOM_INTEGRATION_BASE_URL=$baseUrl"
          Write-Host "AUTH_HEADER=x-api-key"
          Start-Process -FilePath $ts -ArgumentList @("funnel", "--yes", "--bg", "localhost:8787") -WindowStyle Hidden
          Start-Sleep -Seconds 5

      - name: Keep temporary API alive
        if: always()
        shell: pwsh
        run: |
          $minutes = [int]"${{ github.event.inputs.duration_minutes }}"
          if ($minutes -gt 120) { $minutes = 120 }
          if ($minutes -lt 1) { $minutes = 1 }
          $end = (Get-Date).AddMinutes($minutes)
          while ((Get-Date) -lt $end) {
            Write-Host "[$(Get-Date)] Llev approved API active"
            Start-Sleep -Seconds 60
          }
